{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window: core events (the SSE bridge arrives as Tauri events) and the app's own commands. The sidecar and autostart are driven from Rust, so the page needs no shell or autostart permission.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
