---
date: 2026-09-11
project: cc_report
host: wangziyideMacBook-Air
saved_at: 2026-09-14T17:28:16+08:00
tool: cc-center
---

# 2026-09-11 · cc_report

1. **09:55** 設計並實現 Settings 分頁，側欄精簡為 Range 和 Machines
   - 為什麼：每天改動的設定（日期、機器）和偶爾改動的分開，側欄保持專注
   - 怎麼做：側欄只留 Range + Machines，新建 Settings 分頁放四個 group（Collecting/Telling you/Reports/Server），Projects 分頁預設勾選「Only what changed something」
   - commit `(還沒 commit)`

2. **10:07** Agents 加額度和 context meter，Activity 加每日花費日曆
   - 為什麼：開發者需要監看 context 用量、方案的 5h/7d 額度、每日的花費；量表用綠/琥珀/紅三色傳達狀態意義
   - 怎麼做：statusline 鉤子把 Claude Code 的額度寫進 ~/.cc-center/usage.json，scanner 按 API 牌價計算每日成本（token 去重），Agents 加 Plan 區塊和 ctx meter，Activity 上方加 Spend 日曆（GitHub 風格、四階色階、全年 53 週），量表改 width: 100%，加 cache busting `?v=<mtime>`
   - commit `(還沒 commit)`

3. **15:02** 個人設定從代碼移到 .env 文件
   - 為什麼：機器清單、時區、瀏覽器等個人設定不應該在程式碼裡
   - 怎麼做：新建 env.py 讀 .env（標準庫無依賴），修改 settings.py/paths.py/cli.py 載入 .env，修改 bin/cc-center-all 用同套規則讀 .env 並修了 macOS bash 3.2 的兩個 bug，寫 .env.example 作文件
   - commit `(還沒 commit)`
