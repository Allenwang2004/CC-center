# cc-center 的個人設定。複製成 .env (已在 .gitignore) 再改:
#
#   cp .env.example .env
#
# 規則: KEY=value, # 是註解, 值有空白就加引號。已經設好的環境變數永遠優先,
# 這裡只補沒設的。介面 (bin/cc-center-app)、CLI (bin/cc-center) 跟
# bin/cc-center-all 讀的都是這一份。
#
# 這裡的每一項都只是「第一次跑的預設值」—— 介面的 Settings 改過之後會存進
# ~/.cc-center/settings.json, 之後就以那邊為準。

# 要收集的遠端機器, 空白分隔。要在 ~/.ssh/config 有別名、而且能免密碼登入。
# 沒設就只看本機。(~/.cc-center-hosts 一行一台也可以, 而且優先於這裡。)
#CC_HOSTS="server-a server-b"

# 用哪個時區分日: IANA 名稱 (Asia/Taipei、Europe/Berlin) 或固定位移 (+08:00)。
# 沒設就用這台機器的時區。
#CC_TZ=Asia/Taipei

# 開介面用的瀏覽器 (macOS 的 app 名稱, 例如 Arc、Firefox)。空的 = 系統預設。
#CC_BROWSER=Arc

# ssh 連不上多久放棄 (秒)
#CC_SSH_TIMEOUT=8

# 介面的 port (只綁 127.0.0.1)
#CC_CENTER_PORT=8787

# 資料庫、設定、log 放哪 (預設 ~/.cc-center)
#CC_CENTER_STATE=~/.cc-center

# Claude Code 的設定資料夾 (預設 ~/.claude); transcript 從這裡的 projects/ 底下讀
#CLAUDE_CONFIG_DIR=~/.claude

# pip install 到別的地方時, 前端 (web/) 在哪
#CC_CENTER_WEB=

# 你寫的字 (note、journal) 存在 Supabase。下面預設填的是 cc-center 自己的專案:
# 照抄就能用, 用 email 登入後就是你自己的帳號 (row level security 隔開每個人)。
# anon key 本來就是公開的, 保護資料的是 RLS 不是這把 key。
#
# 想用自己的 Supabase 專案: Project Settings → API 拿 Project URL 跟 anon key 換掉,
# SQL Editor 跑一次 supabase/schema.sql 建表, 接一個 SMTP 之後把 Confirm signup
# 跟 Magic Link 兩個 email 樣板加上 {{ .Token }} (登入是寄驗證碼)。
#SUPABASE_URL=
#SUPABASE_ANON_KEY=
